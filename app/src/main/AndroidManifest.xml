<?xml version="1.0" encoding="utf-8"?>
<manifest package="com.gxy.application"
          xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- 读取硬盘信息 -->
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <activity
            android:name=".parclelable.ParcelableActivity"
            android:label="@string/title_activity_main2"
            android:theme="@style/AppTheme">
        </activity>
        <activity
            android:name=".SwipeDismissBehaviorActivity"
            android:label="@string/title_activity_main22"
            android:theme="@style/AppTheme">
        </activity>
        <activity android:name=".AnkoActivity">
        </activity>
        <activity android:name=".KotlinActivity">
        </activity>
        <activity android:name=".fragment.LazyFragmentsActivity">
        </activity>
    </application>

</manifest>